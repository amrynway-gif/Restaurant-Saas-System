-- =============================================================================
-- ربط مستخدم بمطعم (يجب استخدام UUID الحقيقي من مشروعك، وليس أمثلة)
-- =============================================================================
--
-- إذا ظهرت "No rows returned" عند تشغيل SELECT من auth.users
-- فمعناه أنه لا يوجد مستخدم بعد. أنشئ مستخدماً أولاً:
--   Supabase Dashboard → Authentication → Users → Add user → Create new user
--   أدخل البريد الإلكتروني وكلمة المرور ثم Create user.
-- بعدها أعد تشغيل الاستعلامات أدناه.
--
-- =============================================================================

-- الخطوة 1: شغّل الاستعلام التالي لرؤية المستخدمين. انسخ عمود "id" للمستخدم الذي تريده:
SELECT id, email FROM auth.users;

-- الخطوة 2: شغّل الاستعلام التالي لرؤية المطاعم. انسخ عمود "id" للمطعم الذي تريده:
SELECT id, name FROM public.restaurants;

-- الخطوة 3: شغّل INSERT أدناه بعد استبدال القيمتين بالـ id الحقيقي الذي نسخته من الخطوتين 1 و 2:
-- INSERT INTO public.profiles (id, restaurant_id)
-- VALUES (
--   'id-المستخدم-الذي-نسخته-من-auth-users',
--   'id-المطعم-الذي-نسخته-من-restaurants'
-- );
